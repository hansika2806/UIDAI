# Runtime dependencies for the deployed Streamlit dashboard.
# Heavy ML pipeline packages (shap, lightgbm, statsmodels, pyod) are excluded:
# they are only needed when re-running the offline pipeline from raw data.
# In production (precomputed mode) the dashboard only reads pre-built CSVs.

# ── Core data ────────────────────────────────────────────────────────────────
numpy==2.4.4
pandas==2.2.3
scipy==1.17.1

# ── Visualisation & dashboard ────────────────────────────────────────────────
plotly==6.7.0
streamlit==1.57.0

# ── Machine learning (needed at import time by anomaly/clustering modules) ───
scikit-learn==1.6.1
joblib

# ── Data validation ──────────────────────────────────────────────────────────
pandera==0.31.1

# ── Config & logging ─────────────────────────────────────────────────────────
pydantic==2.13.3
pydantic-settings==2.14.0
python-dotenv==1.2.2
python-json-logger==4.1.0

